# ============================================
# 📦 DEPENDENCIAS MULTIPLATAFORMA
# ============================================

# Dependencias principales (todas las plataformas)
Pillow>=9.0.0
qrcode[pil]>=7.0.0
mysql-connector-python>=8.0.0
PyMySQL>=1.0.0

# Dependencias específicas por plataforma
# Windows: Brother QL y win32print
brother_ql; sys_platform == "win32"
pywin32; sys_platform == "win32"

# macOS: posibles fuentes adicionales si se necesitan
# (Por ahora usar fuentes del sistema)

# Linux: Brother QL para impresión directa (opcional)
brother_ql; sys_platform == "linux"

# ============================================
# 📋 INSTRUCCIONES DE INSTALACIÓN PARA LINUX
# ============================================
# Para instalar correctamente en Linux, ejecute primero:
# sudo apt-get update
# sudo apt-get install -y python3-tk python3-dev python3-pil.imagetk
# sudo apt-get install -y python3-pil libjpeg-dev zlib1g-dev libfreetype-dev
# 
# Luego instale las dependencias de Python:
# pip3 install -r requirements.txt
